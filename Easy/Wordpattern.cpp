#include<bits/stdc++.h>
using namespace std;

bool WordPattern(string pattern, string s){
    map<char,string>mp;
    for(int i=0;i<s.length();i++){
        
    }
}

int main()
{
    string s;
    string pattern;
    cin>>pattern>>s;

    bool ans = WordPattern(pattern,s);
    cout<<ans;
    return 0;
}