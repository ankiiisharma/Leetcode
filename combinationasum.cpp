#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int combinationalSum(vector<int>& nums, int target){
    
    int x=0;
    int y=nums.size();
    

    retun 0;
}

int main()
{
    int n,a;
    cin>>n;

    vector<int>nums;
    for(int i=0;i<n;i++){
        cin>>a;
        nums.push_back(a);
    }
    int target;
    cin>>traget;

    combinationalSum(nums,target);

    return 0;
}